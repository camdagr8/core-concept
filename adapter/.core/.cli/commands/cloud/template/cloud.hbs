{{#if collection ~}}
/**
 * ----------------------------------------
 *  Collection Definitions
 * ----------------------------------------
 */
const COLLECTION = '{{collection}}';

{{#if beforeSave ~}}
// BEFORE SAVE {{collection}}
Parse.Cloud.beforeSave(COLLECTION, (req, res) => {

});
{{/if ~}}

{{#if afterSave ~}}
// AFTER SAVE {{collection}}
Parse.Cloud.afterSave(COLLECTION, req => {

});
{{/if ~}}

{{#if beforeDelete ~}}
// BEFORE DELETE {{collection}}
Parse.Cloud.beforeDelete(COLLECTION, (req, res) => {

});
{{/if ~}}

{{#if afterDelete ~}}
// AFTER DELETE {{collection}}
Parse.Cloud.afterDelete(COLLECTION, req => {

});
{{/if ~}}

{{#if beforeFind ~}}
// BEFORE FIND {{collection}}
Parse.Cloud.beforeFind(COLLECTION, req => {

});
{{/if ~}}

{{/if}}

/**
 * ----------------------------------------
 *  Cloud Definitions
 * ----------------------------------------
 */
{{#each definitions}}
Parse.Cloud.define('{{this}}', (req, res) => {

});

{{/each}}
